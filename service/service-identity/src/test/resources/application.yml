spring:
  application:
    name: identity
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:pms;MODE=MYSQL;INIT=CREATE SCHEMA IF NOT EXISTS pms
    username: sa
    password:
  jpa:
    show-sql: true
