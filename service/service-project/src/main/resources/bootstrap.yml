spring:
  cloud:
    nacos:
      config:
        file-extension: yml
        group: PMS_DEV
      discovery:
        group: PMS_DEV
      server-addr: 192.168.2.112:8848
  application:
    name: project