spring:
  application:
    name: storage
  cloud:
    nacos:
      config:
        file-extension: yml
        group: DEFAULT_GROUP
      discovery:
        group: DEFAULT_GROUP
      server-addr: 192.168.2.47:8848